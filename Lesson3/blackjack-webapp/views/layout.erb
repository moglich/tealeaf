<!DOCTYPE html>
<html>
  <head>
    <title>Blackjack - 21</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="stylesheet" href="/vendor/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="/application.css">
  </head>
  <body style="padding-top: 150px;">

  <nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <a class="navbar-brand" href="/">Blackjack</a>

      </div>
        <p class="navbar-text navbar-left">
          <a href="/bet" class="navbar-link">New game</a>
        </p>

        <% if session[:username] %>
          <p class="navbar-text navbar-right">
            Signed in as <strong><%= session[:username] %></strong>. <a href="/logout">logout</a>
          </p>
        <% end %>
      </div>

    </div>
  </nav>

  <div class="container">
    <%if @error %>
      <div class="alert alert-danger"><%= @error %></div>
    <% end %>
    <%if @player_won %>
      <div class="alert alert-success"><%= @player_won %></div>
    <% end %>
    <%if @player_lost %>
      <div class="alert alert-warning"><%= @player_lost %></div>
    <% end %>

    <% if session[:player_status] %>
    <div class="alert alert-warning"><%= session[:player_status] %></div>
    <% end %>

    <%= yield %>
  </div>
  </body>
</html>
